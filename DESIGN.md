---
version: alpha
name: Structured Restraint
description: Neutral, dense product UI with explicit hierarchy and one electric-blue accent.
colors:
  primary-50: "oklch(0.97 0.015 262.56)"
  primary-100: "oklch(0.93 0.04 262.56)"
  primary-200: "oklch(0.87 0.08 262.56)"
  primary-300: "oklch(0.78 0.13 262.56)"
  primary-400: "oklch(0.65 0.19 262.56)"
  primary-500: "oklch(0.5406 0.2549 262.56)"
  primary-600: "oklch(0.48 0.23 262.56)"
  primary-700: "oklch(0.42 0.2 262.56)"
  primary-800: "oklch(0.35 0.16 262.56)"
  primary-900: "oklch(0.28 0.12 262.56)"
  background: "oklch(0.9851 0 0)"
  foreground: "oklch(0.281 0 0)"
  card: "oklch(1 0 0)"
  card-foreground: "oklch(0.281 0 0)"
  popover: "oklch(1 0 0)"
  popover-foreground: "oklch(0.281 0 0)"
  primary: "oklch(0.5406 0.2549 262.56)"
  primary-foreground: "oklch(1 0 0)"
  secondary: "oklch(0.9431 0 0)"
  secondary-foreground: "oklch(0.281 0 0)"
  muted: "oklch(0.97 0 0)"
  muted-foreground: "oklch(0.478 0 0)"
  accent: "oklch(0.9642 0 0)"
  accent-foreground: "oklch(0.281 0 0)"
  destructive: "oklch(0.6256 0.1933 23.03)"
  success: "oklch(0.6271 0.1699 149.21)"
  success-foreground: "oklch(1 0 0)"
  warning: "oklch(0.66 0.18 45)"
  warning-foreground: "oklch(1 0 0)"
  border: "oklch(0.922 0 0)"
  input: "oklch(0.87 0 0)"
  ring: "oklch(0.5406 0.2549 262.56)"
  chart-1: "oklch(0.5406 0.2549 262.56)"
  chart-2: "oklch(0.7 0.13 200)"
  chart-3: "oklch(0.7049 0.1867 47.6)"
  chart-4: "oklch(0.7227 0.192 149.58)"
  chart-5: "oklch(0.62 0.22 295)"
  sidebar: "oklch(0.9851 0 0)"
  sidebar-foreground: "oklch(0.281 0 0)"
  sidebar-primary: "oklch(0.5406 0.2549 262.56)"
  sidebar-primary-foreground: "oklch(1 0 0)"
  sidebar-accent: "oklch(0.9642 0 0)"
  sidebar-accent-foreground: "oklch(0.1776 0 0)"
  sidebar-border: "oklch(0.922 0 0)"
  sidebar-ring: "oklch(0.5406 0.2549 262.56)"
  background-dark: "oklch(0.2002 0 0)"
  foreground-dark: "oklch(1 0 0)"
  card-dark: "oklch(0.2264 0 0)"
  card-foreground-dark: "oklch(1 0 0)"
  popover-dark: "oklch(0.2603 0 0)"
  popover-foreground-dark: "oklch(1 0 0)"
  primary-dark: "oklch(0.5406 0.2549 262.56)"
  primary-foreground-dark: "oklch(1 0 0)"
  secondary-dark: "oklch(0.285 0 0)"
  secondary-foreground-dark: "oklch(0.985 0 0)"
  muted-dark: "oklch(0.2603 0 0)"
  muted-foreground-dark: "oklch(0.6855 0.005 258.33)"
  accent-dark: "oklch(0.256 0 0)"
  accent-foreground-dark: "oklch(1 0 0)"
  destructive-dark: "oklch(0.704 0.191 22.216)"
  success-dark: "oklch(0.7227 0.192 149.58)"
  success-foreground-dark: "oklch(0.2002 0 0)"
  warning-dark: "oklch(0.7455 0.18 55)"
  warning-foreground-dark: "oklch(0.2002 0 0)"
  border-dark: "oklch(1 0 0 / 8%)"
  input-dark: "oklch(1 0 0 / 12%)"
  ring-dark: "oklch(0.6231 0.188 259.81)"
  chart-1-dark: "oklch(0.5406 0.2549 262.56)"
  chart-2-dark: "oklch(0.7 0.13 200)"
  chart-3-dark: "oklch(0.7049 0.1867 47.6)"
  chart-4-dark: "oklch(0.7227 0.192 149.58)"
  chart-5-dark: "oklch(0.62 0.22 295)"
  sidebar-dark: "oklch(0.2002 0 0)"
  sidebar-foreground-dark: "oklch(0.985 0 0)"
  sidebar-primary-dark: "oklch(0.5406 0.2549 262.56)"
  sidebar-primary-foreground-dark: "oklch(1 0 0)"
  sidebar-accent-dark: "oklch(0.2435 0 0)"
  sidebar-accent-foreground-dark: "oklch(1 0 0)"
  sidebar-border-dark: "oklch(1 0 0 / 8%)"
  sidebar-ring-dark: "oklch(0.6231 0.188 259.81)"
typography:
  caption:
    fontFamily: Inter Display
    fontSize: 0.75rem
    fontWeight: 400
    lineHeight: 1.333333
    letterSpacing: -0.15px
  body-sm:
    fontFamily: Inter Display
    fontSize: 0.875rem
    fontWeight: 400
    lineHeight: 1.428571
    letterSpacing: -0.15px
  body:
    fontFamily: Inter Display
    fontSize: 1rem
    fontWeight: 400
    lineHeight: 1.5
    letterSpacing: -0.15px
  label:
    fontFamily: Inter Display
    fontSize: 0.875rem
    fontWeight: 500
    lineHeight: 1.428571
    letterSpacing: -0.15px
  title:
    fontFamily: Inter Display
    fontSize: 1rem
    fontWeight: 600
    lineHeight: 1.5
    letterSpacing: -0.15px
  heading:
    fontFamily: Inter Display
    fontSize: 1.5rem
    fontWeight: 700
    lineHeight: 1.333333
    letterSpacing: -0.15px
  display:
    fontFamily: Inter Display
    fontSize: 3rem
    fontWeight: 500
    lineHeight: 1
    letterSpacing: -0.025em
spacing:
  unit: 0.25rem
  micro: 0.5rem
  control: 0.75rem
  compact: 1rem
  component: 1.5rem
  section: 3rem
  page: 6rem
  page-large: 8rem
rounded:
  sm: 0.5rem
  md: 0.625rem
  lg: 0.75rem
  xl: 1rem
  2xl: 1.25rem
  3xl: 1.5rem
  4xl: 1.75rem
shadows:
  2xs: "0 1px rgb(0 0 0 / 0.05)"
  xs: "0 1px 2px 0 rgb(0 0 0 / 0.05)"
  sm: "0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)"
  md: "0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)"
  lg: "0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)"
  xl: "0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1)"
  2xl: "0 25px 50px -12px rgb(0 0 0 / 0.25)"
  inner: "inset 0 2px 4px 0 rgb(0 0 0 / 0.05)"
---

# Structured Restraint

## Overview

Structured Restraint is a product-first design language for dense workflows. It
should feel calm, exact, and quick to scan. Visual interest comes from hierarchy,
content, and careful alignment rather than decoration.

1. **Dense, not cramped.** Keep related controls close and separate major
   sections with a clear rhythm.
2. **Hierarchy before ornament.** Use type, spacing, borders, and tonal surfaces
   before adding effects.
3. **One accent, clear meaning.** Electric blue marks primary action and focus.
   Status colors communicate state, not decoration.
4. **Composition is a contract.** Use complete component anatomy and predictable
   page structures so behavior and accessibility survive restyling.

## Colors

The palette uses zero-chroma neutrals for the app frame and surfaces. Light mode
starts at near-white `background`; dark mode uses layered charcoal through
`background-dark`, `card-dark`, and `popover-dark`. `primary-500` is the shared
electric-blue accent. Use semantic tokens such as `background`, `foreground`,
`muted-foreground`, `success`, and `warning`; palette steps exist for controlled
variants, not one-off component color choices.

## Typography

Inter Display is the only product typeface. Body copy stays at `body` or
`body-sm`; labels use medium weight; titles use semibold. Reserve `display` for a
single dominant value or page statement. The base `-0.15px` tracking keeps dense
interfaces crisp; large display text uses Tailwind's `tracking-tight`.

## Layout

All spacing derives from Tailwind's `0.25rem` unit. Use named Tailwind spacing
utilities and the rhythm above; never use arbitrary spacing values. Use `gap-*`
on flex or grid containers instead of `space-x-*` or `space-y-*`.

Broad web pages use the repository `container`, capped at `80rem`. Forms and
reading surfaces use the nearest semantic Tailwind `max-w-*` utility. Keep one
primary alignment axis in each section. Page and component anatomy lives in
`.ai/patterns/ui-composition.md`.

## Elevation & Depth

Prefer borders and tonal layers to shadows. Cards sit one surface above the page;
popovers and overlays may use the inherited shadow scale. Use the lowest shadow
that separates the surface. Avoid soft bloom, colored shadows, and stacked
shadow effects.

## Shapes

The base radius is `0.75rem`. Controls use `rounded-lg`, compact labels use
`rounded-sm`, and panels use `rounded-xl` or `rounded-2xl`. Pills may use
`rounded-full` when their shape communicates a chip, badge, or compact action.
Do not mix sharp and highly rounded containers in one composition.

## Components

Use the shadcn/ui components already shipped in `packages/ui` and the mobile
ports in `apps/mobile/src/components/ui`. Compose full slot anatomy for cards,
overlays, avatars, empty states, menus, and grouped controls. App-specific
workflow composition stays in the owning app; shared primitives stay in
`packages/ui`.

Loading placeholders use `Skeleton`. Empty and error states use `Empty`. Use
`Alert` for callouts, `Separator` for structural dividers, and `Badge` for compact
status labels.

## Do's and Don'ts

- Do use semantic color classes. Don't use raw hex, palette colors, or arbitrary
  color values in component class names.
- Do use the spacing scale and a clear alignment axis. Don't use arbitrary
  padding, margins, gaps, or widths to make one screen look right.
- Do compose every required slot. Don't place loose children into a composite or
  reorder its header, content, and footer.
- Do use one page title and one primary action. Don't repeat dashboard titles or
  create competing calls to action.
- Do use `Skeleton` and `Empty` for states. Don't ship raw `animate-pulse`,
  invisible loading gaps, or empty white boxes.
- Never nest cards, add decorative gradients or glows to product workflows, hide
  required overlay titles, omit avatar fallbacks, or leave icon-only buttons
  unnamed.
